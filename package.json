{
  "name": "workspace",
  "private": true,
  "description": "Qullamaggie screening dashboard — root scripts across backend and frontend",
  "scripts": {
    "typecheck": "npm --prefix frontend run typecheck",
    "gen:types": "${PYTHON:-.venv/bin/python} backend/scripts/dump_openapi.py && npm --prefix frontend run gen:types",
    "gen:types:check": "bash scripts/check-types.sh",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && ${PYTHON:-../.venv/bin/python} -m pytest -q",
    "test:frontend": "npm --prefix frontend run test",
    "build": "npm --prefix frontend run build",
    "start": "npm run build && ${PYTHON:-.venv/bin/python} -m uvicorn screener.app:app --app-dir backend --host 127.0.0.1 --port 8000"
  }
}
